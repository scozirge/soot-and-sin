# 專案協作規則

- H5 正式公開內容放在 `soot-and-sin-h5-demo/`。
- 只要回覆涉及程式或素材修改，結尾固定附上公開測試入口與主遊戲網址。
- 純問答、設計討論或沒有修改檔案時，不必附測試網址。
- 不要提交根目錄的 `ideas.md`，除非使用者明確要求。

## 怪物部位破壞圖做法

### 核心原則

- 每隻怪物只能有一張「權威原圖」。禁止讓圖片生成 AI 分別重畫原圖、左手破壞、右手破壞等完整版本；即使提示詞、尺寸和種子相同，分開生成的角色外型仍會漂移，不可使用。
- 圖片數量不是固定三張，而是「一張權威原圖＋每個需要在怪物存活時持續顯示的破壞部位各一張覆蓋圖」。
- 破壞後立刻死亡或立刻結束戰鬥的部位不需要破壞覆蓋圖。例如頭部破壞會即死時，只需要受擊高亮與死亡結果，不製作 `head-destroyed` 圖。

### 覆蓋圖製作

- 所有破壞圖都必須以同一張權威原圖作為編修來源，只能對該部位建立緊貼輪廓的遮罩後局部修補；不可從文字提示重新生成整個人物。
- 圖片生成或修圖工具輸出後，只能擷取遮罩範圍內的修改結果。最終覆蓋圖必須是與原圖畫布尺寸完全相同的透明 PNG，遮罩外 Alpha 必須為 0。
- 覆蓋圖的局部不只是斷口：它必須包含足以完全蓋住原部位的背景、後方身體、衣物與斷口修補，否則底層原部位仍會穿出來。
- 遮罩外不得出現任何半透明殘影，也不得改變臉、姿勢、衣服、光影、比例、背景、鏡頭、其他部位或角色位置。無法保證遮罩外不變的生成結果必須退回重做。
- 各部位遮罩在連接身體的邊界必須盡量小且互不覆蓋；若不可避免，需先定義固定圖層順序，並確認多個部位同時破壞時接縫仍正確。
- 檔名使用 `怪物ID-部位ID-destroyed.png`，部位 ID 必須與程式資料一致。

### 程式疊圖

- 權威原圖永遠顯示在最下層；部位完整時覆蓋圖透明，部位破壞時才淡入對應覆蓋圖。多個部位破壞時同時疊加，不切換成另一張完整人物圖。
- 原圖和全部破壞覆蓋圖必須放在同一個 `monsterArt` 容器，使用相同的 `inset`、寬高和 `object-fit`。受擊放大、位移及攻擊動畫只能套在共同容器，禁止單獨變形某一張圖。
- 部位選取高亮圖、點擊遮罩與永久破壞圖必須分開。高亮圖只顯示滑鼠指向和選取效果，不能拿來充當破壞狀態。

### 必須驗證

- 素材驗證：原圖與所有覆蓋圖畫布尺寸相同；覆蓋圖在允許遮罩外的 Alpha 為 0。只檢查寬高相同不算通過。
- 畫面驗證：逐一截圖比較「未破壞、每個單一部位破壞、所有可共存部位同時破壞」。除了被破壞的部位與必要接縫外，其他像素和角色輪廓必須保持權威原圖不變。
- 動畫驗證：受擊放大期間原圖與所有已顯示覆蓋圖必須一起移動、一起縮放，不能錯位或露出原部位。
- 惡魔目前的正確配置是三張：`soot-hound.webp`、左臂破壞覆蓋圖、右臂破壞覆蓋圖。頭部破壞會即死，所以沒有頭部破壞覆蓋圖。

# 專案協作規則

- H5 正式公開內容放在 `soot-and-sin-h5-demo/`。
- 只要回覆涉及程式或素材修改，結尾固定附上公開測試入口與主遊戲網址。
- 純問答、設計討論或沒有修改檔案時，不必附測試網址。
- 不要提交根目錄的 `ideas.md`，除非使用者明確要求。

## 怪物部位破壞圖做法

### 核心原則

- 每隻怪物只能有一張「權威原圖」。禁止讓圖片生成 AI 分別重畫原圖、左手破壞、右手破壞等完整版本；即使提示詞、尺寸和種子相同，分開生成的角色外型仍會漂移，不可使用。
- 圖片數量不是固定三張，而是「一張權威原圖＋每個需要在怪物存活時持續顯示的破壞部位各一張覆蓋圖」。
- 破壞後立刻死亡或立刻結束戰鬥的部位不需要破壞覆蓋圖。例如頭部破壞會即死時，只需要受擊高亮與死亡結果，不製作 `head-destroyed` 圖。

### 覆蓋圖製作

- 所有破壞圖都必須以同一張權威原圖作為編修來源，只能對該部位建立緊貼輪廓的遮罩後局部修補；不可從文字提示重新生成整個人物。
- 圖片生成或修圖工具輸出後，只能擷取遮罩範圍內的修改結果。最終覆蓋圖必須是與原圖畫布尺寸完全相同的透明 PNG，遮罩外 Alpha 必須為 0。
- 覆蓋圖的局部不只是斷口：它必須包含足以完全蓋住原部位的背景、後方身體、衣物與斷口修補，否則底層原部位仍會穿出來。
- 遮罩外不得出現任何半透明殘影，也不得改變臉、姿勢、衣服、光影、比例、背景、鏡頭、其他部位或角色位置。無法保證遮罩外不變的生成結果必須退回重做。
- 各部位遮罩在連接身體的邊界必須盡量小且互不覆蓋；若不可避免，需先定義固定圖層順序，並確認多個部位同時破壞時接縫仍正確。
- 檔名使用 `怪物ID-部位ID-destroyed.png`，部位 ID 必須與程式資料一致。
- 惡魔斷肢圖的實際流程是唯一基準：先以精細部位遮罩鎖定整條肢體，再對權威原圖做局部移除／補景；圖片生成結果只可作為遮罩內的修補來源，不能直接當完整狀態圖使用。
- 最終斷肢覆蓋圖必須從「編修後圖片－精細允許遮罩」擷取到同尺寸透明畫布。惡魔使用約 25px 向外擴張及 4.5px 羽化作為接縫參考；其他尺寸需等比例調整，不能用矩形裁切框代替遮罩。
- 完成後必須把覆蓋圖壓回權威原圖，以原始解析度檢查斷口、補景、殘肢及接縫。縮圖看起來相似不算通過。

### 程式疊圖

- 權威原圖永遠顯示在最下層；部位完整時覆蓋圖透明，部位破壞時才淡入對應覆蓋圖。多個部位破壞時同時疊加，不切換成另一張完整人物圖。
- 原圖和全部破壞覆蓋圖必須放在同一個 `monsterArt` 容器，使用相同的 `inset`、寬高和 `object-fit`。受擊放大、位移及攻擊動畫只能套在共同容器，禁止單獨變形某一張圖。
- 部位選取高亮圖、點擊遮罩與永久破壞圖必須分開。高亮圖只顯示滑鼠指向和選取效果，不能拿來充當破壞狀態。

### 必須驗證

- 素材驗證：原圖與所有覆蓋圖畫布尺寸相同；覆蓋圖在允許遮罩外的 Alpha 為 0。只檢查寬高相同不算通過。
- 畫面驗證：逐一截圖比較「未破壞、每個單一部位破壞、所有可共存部位同時破壞」。除了被破壞的部位與必要接縫外，其他像素和角色輪廓必須保持權威原圖不變。
- 動畫驗證：受擊放大期間原圖與所有已顯示覆蓋圖必須一起移動、一起縮放，不能錯位或露出原部位。
- 惡魔目前的正確配置是三張：`soot-hound.webp`、左臂破壞覆蓋圖、右臂破壞覆蓋圖。頭部破壞會即死，所以沒有頭部破壞覆蓋圖。

## 新增怪物完整戰鬥的交付規格

### 開始前

- 先閱讀目前惡魔的戰鬥程式、素材結構及驗證腳本，把惡魔視為功能基準，但不可直接複製惡魔的部位座標、遮罩、數值或圖片。
- 實作前先列出該怪物的部位、每個部位是否可破壞、破壞門檻、破壞效果、是否即死，以及全部怪物行動。資料不足時必須先詢問，不可自行補成既定設計。
- 新怪物必須使用資料驅動設定。部位、行動、素材路徑及座標放在怪物設定資料中，禁止在共用 UI 函式內新增怪物名稱判斷或散落的特例。

### 每隻怪物必須提供的資料

- 怪物：`id`、顯示名稱、最大生命、權威原圖、原圖畫布尺寸。
- 部位：`id`、名稱、SVG 點擊路徑、命中率、傷害倍率、破壞門檻、跳字座標、標示線座標、是否即死、暈眩時間及其他破壞效果。
- 行動：`id`、名稱、傷害、倒數時間、目標規則，以及 `requiresAny` 或 `requiresAll` 所需部位。
- 素材：每個部位的點擊遮罩；只有怪物破壞後仍存活的部位才需要永久破壞覆蓋圖。
- 部位、行動與素材使用的 ID 必須完全一致，不可在程式中靠中文名稱配對。

### 部位點擊與紅色高亮

- SVG 點擊路徑必須依權威原圖逐部位描繪，`viewBox` 必須等於原圖畫布。路徑要貼合部位輪廓，不可使用矩形、橢圓或大範圍近似形狀代替。
- 各部位點擊區不可錯位或不必要地互相重疊。重疊處必須明確規定優先順序；身體外的透明區域不可點擊。
- SVG 路徑只負責點擊判定，不可直接把粗略 SVG 區塊填成紅色當作最終高亮。視覺高亮必須另外使用與惡魔相同的全解析度 Alpha 遮罩，因此不能出現矩形邊、直線裁切邊或整塊半透明方框。
- 惡魔高亮的實際流程位於 `soot-and-sin-h5-demo/soot-and-sin-event-prototypes/battle-event/tools/build_hit_overlays.py`：在權威原圖的兩倍解析度，以部位外框多邊形加前景導引線執行 GrabCut，接著閉合細縫、輕微羽化並縮回原尺寸。新怪物必須沿用這個流程並逐部位調整多邊形和導引線，直到遮罩精細貼合髮絲、手指、衣袖、肢體與空隙。
- 每個部位必須輸出一張全畫布灰階 `怪物ID-部位ID-mask.png`。遮罩內是部位，遮罩外必須為 0；輪廓只能有少量抗鋸齒羽化，不可包含遮罩包圍盒中的背景。
- 高亮 PNG 必須由「權威原圖原始像素＋該部位 Alpha 遮罩」確定性產生。使用 Overlay 混合保留明暗、材質與細節，再套用接近 `#d98d53` 的選取色；禁止讓圖片生成 AI、矩形裁切或純色 SVG 重畫部位。
- 高亮 PNG、遮罩 PNG 與權威原圖畫布必須完全相同。高亮 PNG 的 Alpha 必須等於對應遮罩，遮罩外 Alpha 為 0。高亮圖不是破壞圖，兩者不得共用。
- 製作時必須參考 `soot-and-sin-h5-demo/soot-and-sin-adventure-demo/battle/assets/hit-overlays/` 內的惡魔成品：`soot-hound-head-mask.png`、`soot-hound-left_limb-mask.png`、`soot-hound-right_limb-mask.png` 及其同名高亮 PNG。若新遮罩的輪廓品質明顯低於惡魔，必須重做，禁止提交。
- 未選擇攻擊行動時不顯示部位高亮；滑鼠指向時顯示較亮高亮；選取時使用明顯的 `#d98d53` 色調。防禦、閃避等非部位行動不可選取部位。
- 滑鼠指向哪個部位，只能顯示該部位的高亮和資訊。已破壞部位不可再次選取。

### 部位資訊與標示線

- 每個怪物、每個部位都必須有獨立的標示線佈局；禁止複製惡魔或另一隻怪物的座標。頭部靠近畫面上緣時，標示線要改往左、右或下方延伸，不能繼續往上超框。
- 標示線端點要落在部位內，文字要盡量放在怪物外側，但整條線、圓點及全部文字仍必須位於怪物畫布 `viewBox` 內。
- 產生文字後必須用 SVG `getBBox()` 取得實際範圍並做邊界限制。線與文字的合併範圍必須保持至少 24px 安全邊距：`x >= 24`、`y >= 24`、`x + width <= viewBoxWidth - 24`、`y + height <= viewBoxHeight - 24`。
- 超出安全範圍時必須重新選擇標示方向或調整文字錨點，禁止單純使用 `overflow: visible` 讓內容溢出，也禁止縮小到難以閱讀。
- 命中率與傷害倍率文字不可被意圖卡、畫面邊界或其他 UI 裁切；桌面及手機版都必須逐部位實際指向並截圖檢查。

### 戰鬥回饋與部位破壞

- 命中、傷害、失誤、暈眩與部位破壞文字必須出現在實際受擊部位的跳字座標，不能使用全怪物共用固定位置。
- 一般跳字字級以目前惡魔的 24px 為基準；「部位破壞」必須有明顯獨立標示。永久破壞覆蓋圖以目前 0.48 秒淡入效果為基準。
- 部位破壞後立刻停用該部位。正在執行且依賴該部位的玩家、隊友或怪物行動必須取消；怪物下一步只能從仍符合部位需求的行動中選擇。
- `requiresAll` 代表所有列出的部位都存在才能使用；`requiresAny` 代表至少一個列出的部位存在。不可把兩者混用。
- 即死部位達到破壞門檻時直接把怪物生命歸零並結束戰鬥，不顯示不存在的常駐破壞狀態，也不要求即死部位破壞覆蓋圖。
- 切換怪物或重新開始戰鬥時，必須清除上一隻怪物的選取、高亮、破壞圖層、部位揭露、行動與動畫狀態。

### 強制驗收矩陣

- 自動檢查每張素材的尺寸、Alpha 範圍、檔名與設定 ID；遮罩外出現非透明像素即失敗。高亮 PNG 的 Alpha 與精細遮罩不一致，或斷肢圖 Alpha 超出允許遮罩時也必須失敗。
- 自動檢查高亮遮罩不得退化成部位包圍盒。遮罩必須保留肢體空隙及凹凸輪廓；任何可見矩形邊、整塊背景染色或直線截斷都視為失敗。
- 對每個部位測試滑鼠指向、點擊、鍵盤操作、顯示資訊及跳字座標，並確認不會誤觸其他部位。
- 對每個部位呼叫 `getBBox()` 驗證標示線與文字完整位於 24px 安全範圍內；任何超框、裁切或與上方意圖卡重疊都視為失敗。
- 對每個非即死部位分別測試：未破壞、剛破壞、破壞後不可選、破壞圖淡入、相關行動取消及剩餘行動合法。
- 測試所有可以同時存在的破壞部位組合，確認覆蓋圖沒有接縫、殘肢、原部位穿出或角色外型改變。
- 對即死部位測試門檻前仍存活、達門檻立即死亡，且程式不會載入或要求不存在的即死部位破壞圖。
- 測試玩家與隊友的命中及失誤；文字必須出現在所攻擊部位。測試怪物受擊動畫時所有可見圖層共同縮放。
- 至少產出並以原始解析度人工檢查以下截圖：初始畫面、每個部位指向、每個精細高亮遮罩、每個單一破壞狀態、所有可共存破壞狀態、部位破壞跳字，以及桌面與手機版面。只看素材縮圖或只檢查程式數值禁止視為完成。
- 執行 JavaScript 語法檢查、該怪物專用自動驗證及既有惡魔回歸驗證。新增怪物不得讓惡魔原本功能失效。
- 任何一項驗收未通過、沒有實際截圖或只用程式碼推測畫面正確時，禁止宣稱完成、禁止提交、禁止推送公開版本；必須修正後重新跑完整驗收。
